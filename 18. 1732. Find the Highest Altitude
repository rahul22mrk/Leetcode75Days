class Solution {
    public int largestAltitude(int[] gain) {
        int highest=0,sum=0;
        for(int num:gain){
            sum =sum+num;
            highest = Math.max(sum,highest);
        }
        return highest;

    }
}
